
type Props = {
  name: string,
  role: string
}

const Card = ({name, role} : Props) => {
  return (
    <div>
      <p>aojsn gwfw</p>
      <p>{name}</p>
      <p>{role}</p>
    </div>
  )
}

export default Card
